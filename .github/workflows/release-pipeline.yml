name: Higher Environment Release Pipeline

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:

  deploy-prod:
    name: Deploy Production
    uses: anilkumar-repo/enterprise-ci-cd-workflows/.github/workflows/deploy.yml@main
    with:
      deploy_provider: netlify
      environment: prod
      publish_dir: .
    secrets:
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      NETLIFY_SITE_ID: ${{ secrets.NETLIFY_PROD_SITE_ID }}

  rollback-prod:
    name: Rollback Production
    if: ${{ always() && needs.deploy-prod.result == 'failure' }}
    needs: deploy-prod
    uses: anilkumar-repo/enterprise-ci-cd-workflows/.github/workflows/rollback.yml@main
    with:
      deploy_provider: netlify
      environment: prod

  create-release:
    name: Create GitHub Release
    if: ${{ needs.deploy-prod.result == 'success' }}
    needs: deploy-prod
    runs-on: ubuntu-latest

    steps:
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body: |
            ## Enterprise Portfolio Release

            ### Deployment Summary
            - Production deployment completed successfully
            - Enterprise CI/CD pipeline executed
            - Release version validated

            ### Included Updates
            - UI/UX enhancements
            - Workflow optimizations
            - Portfolio improvements
            - Performance enhancements

            ### Deployment Information
            - Environment: Production
            - Deployment Strategy: GitHub Actions
            - Release Version: ${{ github.ref_name }}

            ### Author
            Anil Kumar M